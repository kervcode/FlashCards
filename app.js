const express = require('express');

const app = express() //return an express application.

app.get(); //create a route for the root route the the flashcard app.

app.listen(3000) //listen method is use to setup the dev setup. It took one param {the port number}.